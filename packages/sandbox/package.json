{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Secure sandbox for interactive documents renderer",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "vite",
    "prebuild": "node scripts/resources.js",
    "tsc": "tsc -p .",
    "bundle": "vite build --config vite.bundle.config.js",
    "build": "npm run tsc && npm run bundle",
    "build:02": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan Marshall",
  "license": "MIT"
}
