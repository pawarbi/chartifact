import * as editor from 'editor';
import * as compiler from 'compiler';
import * as sandbox from 'sandbox';
import * as host from 'host';

export as namespace IDocs;

export { editor, compiler, sandbox, host };
